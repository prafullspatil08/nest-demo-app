// It's crucial to keep this secret out of the source code in a real application.
// Use environment variables or a secrets management service.
export const jwtConstants = {
  secret: 'yourSecretKey', // Replace with a strong, unique secret key
};
